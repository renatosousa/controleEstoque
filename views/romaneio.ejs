<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
      <link rel='stylesheet' href='/stylesheets/style.css' />
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
        <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.15/angular.min.js"></script>
        <script src="javascripts/romaneio.js"></script>
  </head>
  <body ng-app="appRomaneio">
    <div class="container ng-scope" ng-controller="ComprimentosController">
        <div class="row">
            <div class="page-header">
              <h1><%= title %></h1>
            </div>
        </div>

        <div class="row">
            <form class="form-inline ng-pristine ng-valid" name="formItem">
                <input type="number" ng-model="item.comprimento" placeholder="Comprimento" class="ng-pristine ng-valid">

                <button class="btn btn-primary" ng-click="adicionaComprimento()">add</button>
            </form>
        </div>

        <div class="row">
            <table id="lista-compras" class="table table-striped">
                <thead>
                <tr>
                    <th>Comprimento</th>
                    <th>Quantidade</th>
                </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="item in itens" class="comprado-false">
                        <td><strong class="ng-binding" > {{ item.comprimento }}</strong></td>
                        <td><input type="number" ng-model="item.quantidade" ng-change="change()" class="ng-pristine ng-valid"></td>
                    </tr>
               </tbody>
                <tfoot>
                <tr>
                    <td> Total: </td>
                    <td class="ng-binding"> {{ total }} cm3 </td>
                </tr>
                </tfoot>
            </table>
        </div>


    </div>
  </body>
</html>
